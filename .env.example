# GitHub Personal Access Token (classic with public_repo / read:user permission)
# Optional but recommended to avoid rate limits (60 req/hour -> 5000 req/hour)
GITHUB_TOKEN=your_github_personal_access_token_here

# Server Port
PORT=3000

# Cache Duration in seconds (default 3600 = 1 hour)
CACHE_TTL=3600

